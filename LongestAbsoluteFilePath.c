#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
int lengthLongestPath(char* input);

int main(){
	char* test = "cocaine";
	printf("%d",test[0] == 'o');
	
}


int lengthLongestPath(char* input) {
	
	int length = sizeof(input)/sizeof(char), i=0;
	
	char *array [
	
	
	while(i < length){
		if(input[i] == '\' && input[i+1] == 'n'){
		
		}
		
	
	}
 
	
	while(i < length){
		if(input[i] == '.'){ //found a file
			int j = i;
			
			while(j>0 && input[j] != 't' && input[j-1] != '\'){
				j--;
			}
			if(j==0) //error, we did not find a 
				
			
		}
	}
    
}
